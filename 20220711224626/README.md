# how to run curl silently

Pretty obvious after I find it (like everything else) but just recording to make it easier

This run without the "downloading" arrow thingy, great for running within a vi session via [magic wands](https://rwx.gg/vimagic)

```sh
curl -s <url>
```

    #curl #shell
